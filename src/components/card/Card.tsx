import React, { ReactNode } from 'react';

interface CardTtype {
  content: string;
  imgsrc: string;
}

const Card: React.FC<CardTtype> = ({ content, imgsrc }) => {
  return <div></div>;
};

export default Card;
